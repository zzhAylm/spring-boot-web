spring:
  application:
    name: SPRING-BOOT-STREAMS
  profiles:
    active: ${profile:test}
  security:
    user:
      name: admin
      password: 123456
  servlet:
    multipart:
      max-request-size: 50MB
      max-file-size: 50MB
server:
  port: ${port:8080}

